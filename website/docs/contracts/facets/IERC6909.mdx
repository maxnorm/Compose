---
sidebar_position: 99
title: "IERC6909"
description: "**Title:**"
---

import DocHero from '@site/src/components/docs/DocHero';
import Callout from '@site/src/components/ui/Callout';
import APIReference from '@site/src/components/api/APIReference';

<DocHero
  title="IERC6909"
  subtitle="**Title:**"
  variant="gradient"
  height="small"
/>

## Overview

**Title:**

ERC-6909 Minimal Multi-Token Interface

[View Source](https://github.com/maxnorm/Compose/blob/ff0903436c4e0119526128efec4dad880d20e793/src/interfaces/IERC6909.sol)

## Functions

### balanceOf

Owner balance of an id.

<APIReference
  method="GET"
  endpoint="function balanceOf(address _owner, uint256 _id) external view returns (uint256);"
  description="Owner balance of an id."
  parameters={[
      {
          "name": "_owner",
          "type": "address",
          "required": true,
          "description": "The address of the owner."
      },
      {
          "name": "_id",
          "type": "uint256",
          "required": true,
          "description": "The id of the token."
      }
  ]}
  response={{
      "result": "The balance of the token."
  }}
/>

### allowance

Spender allowance of an id.

<APIReference
  method="GET"
  endpoint="function allowance(address _owner, address _spender, uint256 _id) external view returns (uint256);"
  description="Spender allowance of an id."
  parameters={[
      {
          "name": "_owner",
          "type": "address",
          "required": true,
          "description": "The address of the owner."
      },
      {
          "name": "_spender",
          "type": "address",
          "required": true,
          "description": "The address of the spender."
      },
      {
          "name": "_id",
          "type": "uint256",
          "required": true,
          "description": "The id of the token."
      }
  ]}
  response={{
      "result": "The allowance of the token."
  }}
/>

### isOperator

Checks if a spender is approved by an owner as an operator.

<APIReference
  method="GET"
  endpoint="function isOperator(address _owner, address _spender) external view returns (bool);"
  description="Checks if a spender is approved by an owner as an operator."
  parameters={[
      {
          "name": "_owner",
          "type": "address",
          "required": true,
          "description": "The address of the owner."
      },
      {
          "name": "_spender",
          "type": "address",
          "required": true,
          "description": "The address of the spender."
      }
  ]}
  response={{
      "result": "The approval status."
  }}
/>

### approve

Approves an amount of an id to a spender.

<APIReference
  method="POST"
  endpoint="function approve(address _spender, uint256 _id, uint256 _amount) external returns (bool);"
  description="Approves an amount of an id to a spender."
  parameters={[
      {
          "name": "_spender",
          "type": "address",
          "required": true,
          "description": "The address of the spender."
      },
      {
          "name": "_id",
          "type": "uint256",
          "required": true,
          "description": "The id of the token."
      },
      {
          "name": "_amount",
          "type": "uint256",
          "required": true,
          "description": "The amount of the token."
      }
  ]}
  response={{
      "result": "Whether the approval succeeded."
  }}
/>

### setOperator

Sets or removes a spender as an operator for the caller.

<APIReference
  method="POST"
  endpoint="function setOperator(address _spender, bool _approved) external returns (bool);"
  description="Sets or removes a spender as an operator for the caller."
  parameters={[
      {
          "name": "_spender",
          "type": "address",
          "required": true,
          "description": "The address of the spender."
      },
      {
          "name": "_approved",
          "type": "bool",
          "required": true,
          "description": "The approval status."
      }
  ]}
  response={{
      "result": "Whether the operator update succeeded."
  }}
/>

### transfer

Transfers an amount of an id from the caller to a receiver.

<APIReference
  method="POST"
  endpoint="function transfer(address _receiver, uint256 _id, uint256 _amount) external returns (bool);"
  description="Transfers an amount of an id from the caller to a receiver."
  parameters={[
      {
          "name": "_receiver",
          "type": "address",
          "required": true,
          "description": "The address of the receiver."
      },
      {
          "name": "_id",
          "type": "uint256",
          "required": true,
          "description": "The id of the token."
      },
      {
          "name": "_amount",
          "type": "uint256",
          "required": true,
          "description": "The amount of the token."
      }
  ]}
  response={{
      "result": "Whether the transfer succeeded."
  }}
/>

### transferFrom

Transfers an amount of an id from a sender to a receiver.

<APIReference
  method="POST"
  endpoint="function transferFrom(address _sender, address _receiver, uint256 _id, uint256 _amount) external returns (bool);"
  description="Transfers an amount of an id from a sender to a receiver."
  parameters={[
      {
          "name": "_sender",
          "type": "address",
          "required": true,
          "description": "The address of the sender."
      },
      {
          "name": "_receiver",
          "type": "address",
          "required": true,
          "description": "The address of the receiver."
      },
      {
          "name": "_id",
          "type": "uint256",
          "required": true,
          "description": "The id of the token."
      },
      {
          "name": "_amount",
          "type": "uint256",
          "required": true,
          "description": "The amount of the token."
      }
  ]}
  response={{
      "result": "Whether the transfer succeeded."
  }}
/>

## Events

### Transfer

```solidity
event Transfer(
    address _caller, address indexed _sender, address indexed _receiver, uint256 indexed _id, uint256 _amount
);
```

### OperatorSet

```solidity
event OperatorSet(address indexed _owner, address indexed _spender, bool _approved);
```

### Approval

```solidity
event Approval(address indexed _owner, address indexed _spender, uint256 indexed _id, uint256 _amount);
```

## Errors

### ERC6909InsufficientBalance

```solidity
error ERC6909InsufficientBalance(address _sender, uint256 _balance, uint256 _needed, uint256 _id);
```

### ERC6909InsufficientAllowance

```solidity
error ERC6909InsufficientAllowance(address _spender, uint256 _allowance, uint256 _needed, uint256 _id);
```

### ERC6909InvalidApprover

```solidity
error ERC6909InvalidApprover(address _approver);
```

### ERC6909InvalidReceiver

```solidity
error ERC6909InvalidReceiver(address _receiver);
```

### ERC6909InvalidSender

```solidity
error ERC6909InvalidSender(address _sender);
```

### ERC6909InvalidSpender

```solidity
error ERC6909InvalidSpender(address _spender);
```

