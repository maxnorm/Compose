---
sidebar_position: 99
title: ERC20Facet
description: API documentation for ERC20Facet
---

import DocHero from '@site/src/components/docs/DocHero';
import Callout from '@site/src/components/ui/Callout';
import APIReference from '@site/src/components/api/APIReference';

<DocHero
  title="ERC20Facet"
  subtitle="API documentation for ERC20Facet"
  variant="gradient"
  height="small"
/>

## Overview

Documentation for ERC20Facet.

[View Source](https://github.com/maxnorm/Compose/blob/6956906c9ed86d52ca90b8f2deea310beff37f02/src/token/ERC20/ERC20/ERC20Facet.sol)

## Functions

### getStorage

Returns the ERC20 storage struct from the predefined diamond storage slot. Uses inline assembly to set the storage slot reference.

<APIReference
  method="GET"
  endpoint="function getStorage() internal pure returns (ERC20Storage storage s);"
  description="Returns the ERC20 storage struct from the predefined diamond storage slot. Uses inline assembly to set the storage slot reference."
  parameters={[]}
  response={{
      "s": "The ERC20 storage struct reference."
  }}
/>

### name

Returns the name of the token.

<APIReference
  method="GET"
  endpoint="function name() external view returns (string memory);"
  description="Returns the name of the token."
  parameters={[]}
  response={{
      "result": "The token name."
  }}
/>

### symbol

Returns the symbol of the token.

<APIReference
  method="GET"
  endpoint="function symbol() external view returns (string memory);"
  description="Returns the symbol of the token."
  parameters={[]}
  response={{
      "result": "The token symbol."
  }}
/>

### decimals

Returns the number of decimals used for token precision.

<APIReference
  method="GET"
  endpoint="function decimals() external view returns (uint8);"
  description="Returns the number of decimals used for token precision."
  parameters={[]}
  response={{
      "result": "The number of decimals."
  }}
/>

### totalSupply

Returns the total supply of tokens.

<APIReference
  method="GET"
  endpoint="function totalSupply() external view returns (uint256);"
  description="Returns the total supply of tokens."
  parameters={[]}
  response={{
      "result": "The total token supply."
  }}
/>

### balanceOf

Returns the balance of a specific account.

<APIReference
  method="GET"
  endpoint="function balanceOf(address _account) external view returns (uint256);"
  description="Returns the balance of a specific account."
  parameters={[
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The address of the account."
      }
  ]}
  response={{
      "result": "The account balance."
  }}
/>

### allowance

Returns the remaining number of tokens that a spender is allowed to spend on behalf of an owner.

<APIReference
  method="GET"
  endpoint="function allowance(address _owner, address _spender) external view returns (uint256);"
  description="Returns the remaining number of tokens that a spender is allowed to spend on behalf of an owner."
  parameters={[
      {
          "name": "_owner",
          "type": "address",
          "required": true,
          "description": "The address of the token owner."
      },
      {
          "name": "_spender",
          "type": "address",
          "required": true,
          "description": "The address of the spender."
      }
  ]}
  response={{
      "result": "The remaining allowance."
  }}
/>

### approve

Approves a spender to transfer up to a certain amount of tokens on behalf of the caller. Emits an [Approval](/src/token/ERC20/ERC20/ERC20Facet.sol/contract.ERC20Facet.md#approval) event.

<APIReference
  method="POST"
  endpoint="function approve(address _spender, uint256 _value) external returns (bool);"
  description="Approves a spender to transfer up to a certain amount of tokens on behalf of the caller. Emits an [Approval](/src/token/ERC20/ERC20/ERC20Facet.sol/contract.ERC20Facet.md#approval) event."
  parameters={[
      {
          "name": "_spender",
          "type": "address",
          "required": true,
          "description": "The address approved to spend tokens."
      },
      {
          "name": "_value",
          "type": "uint256",
          "required": true,
          "description": "The number of tokens to approve."
      }
  ]}
  response={{
      "result": "True if the approval was successful."
  }}
/>

### transfer

Transfers tokens to another address. Emits a [Transfer](/src/token/ERC20/ERC20/ERC20Facet.sol/contract.ERC20Facet.md#transfer) event.

<APIReference
  method="POST"
  endpoint="function transfer(address _to, uint256 _value) external returns (bool);"
  description="Transfers tokens to another address. Emits a [Transfer](/src/token/ERC20/ERC20/ERC20Facet.sol/contract.ERC20Facet.md#transfer) event."
  parameters={[
      {
          "name": "_to",
          "type": "address",
          "required": true,
          "description": "The address to receive the tokens."
      },
      {
          "name": "_value",
          "type": "uint256",
          "required": true,
          "description": "The amount of tokens to transfer."
      }
  ]}
  response={{
      "result": "True if the transfer was successful."
  }}
/>

### transferFrom

Transfers tokens on behalf of another account, provided sufficient allowance exists. Emits a [Transfer](/src/token/ERC20/ERC20/ERC20Facet.sol/contract.ERC20Facet.md#transfer) event and decreases the spender's allowance.

<APIReference
  method="POST"
  endpoint="function transferFrom(address _from, address _to, uint256 _value) external returns (bool);"
  description="Transfers tokens on behalf of another account, provided sufficient allowance exists. Emits a [Transfer](/src/token/ERC20/ERC20/ERC20Facet.sol/contract.ERC20Facet.md#transfer) event and decreases the spender's allowance."
  parameters={[
      {
          "name": "_from",
          "type": "address",
          "required": true,
          "description": "The address to transfer tokens from."
      },
      {
          "name": "_to",
          "type": "address",
          "required": true,
          "description": "The address to transfer tokens to."
      },
      {
          "name": "_value",
          "type": "uint256",
          "required": true,
          "description": "The amount of tokens to transfer."
      }
  ]}
  response={{
      "result": "True if the transfer was successful."
  }}
/>

## Events

### Approval

Emitted when an approval is made for a spender by an owner.

```solidity
event Approval(address indexed _owner, address indexed _spender, uint256 _value);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_owner` | `address` | The address granting the allowance. |
| `_spender` | `address` | The address receiving the allowance. |
| `_value` | `uint256` | The amount approved. |

### Transfer

Emitted when tokens are transferred between two addresses.

```solidity
event Transfer(address indexed _from, address indexed _to, uint256 _value);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_from` | `address` | Address sending the tokens. |
| `_to` | `address` | Address receiving the tokens. |
| `_value` | `uint256` | Amount of tokens transferred. |

## Errors

### ERC20InsufficientBalance

Thrown when an account has insufficient balance for a transfer or burn.

```solidity
error ERC20InsufficientBalance(address _sender, uint256 _balance, uint256 _needed);
```

### ERC20InvalidSender

Thrown when the sender address is invalid (e.g., zero address).

```solidity
error ERC20InvalidSender(address _sender);
```

### ERC20InvalidReceiver

Thrown when the receiver address is invalid (e.g., zero address).

```solidity
error ERC20InvalidReceiver(address _receiver);
```

### ERC20InsufficientAllowance

Thrown when a spender tries to use more than the approved allowance.

```solidity
error ERC20InsufficientAllowance(address _spender, uint256 _allowance, uint256 _needed);
```

### ERC20InvalidSpender

Thrown when the spender address is invalid (e.g., zero address).

```solidity
error ERC20InvalidSpender(address _spender);
```

## Structs

### ERC20Storage

```solidity
struct ERC20Storage {
    mapping(address owner => uint256 balance) balanceOf;
    uint256 totalSupply;
    mapping(address owner => mapping(address spender => uint256 allowance)) allowance;
    uint8 decimals;
    string name;
    string symbol;
}
```

