---
sidebar_position: 99
title: "AccessControlPausableFacet"
description: "API documentation for AccessControlPausableFacet"
---

import DocHero from '@site/src/components/docs/DocHero';
import Callout from '@site/src/components/ui/Callout';
import APIReference from '@site/src/components/api/APIReference';

<DocHero
  title="AccessControlPausableFacet"
  subtitle="API documentation for AccessControlPausableFacet"
  variant="gradient"
  height="small"
/>

## Overview

Documentation for AccessControlPausableFacet.

[View Source](https://github.com/maxnorm/Compose/blob/4c4748fcb0d24a68579be4c02891d5ceb2800314/src/access/AccessControlPausable/AccessControlPausableFacet.sol)

## Functions

### getAccessControlStorage

Returns the storage for AccessControl.

<APIReference
  method="GET"
  endpoint="function getAccessControlStorage() internal pure returns (AccessControlStorage storage s);"
  description="Returns the storage for AccessControl."
  parameters={[]}
  response={{
      "s": "The AccessControl storage struct."
  }}
/>

### getStorage

Returns the storage for AccessControlPausable.

<APIReference
  method="GET"
  endpoint="function getStorage() internal pure returns (AccessControlPausableStorage storage s);"
  description="Returns the storage for AccessControlPausable."
  parameters={[]}
  response={{
      "s": "The AccessControlPausable storage struct."
  }}
/>

### isRolePaused

Returns if a role is paused.

<APIReference
  method="GET"
  endpoint="function isRolePaused(bytes32 _role) external view returns (bool);"
  description="Returns if a role is paused."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to check."
      }
  ]}
  response={{
      "result": "True if the role is paused, false otherwise."
  }}
/>

### pauseRole

Temporarily disables a role, preventing all accounts from using it. Only the admin of the role can pause it. Emits a [RolePaused](/src/access/AccessControlPausable/AccessControlPausableFacet.sol/contract.AccessControlPausableFacet.md#rolepaused) event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="function pauseRole(bytes32 _role) external;"
  description="Temporarily disables a role, preventing all accounts from using it. Only the admin of the role can pause it. Emits a [RolePaused](/src/access/AccessControlPausable/AccessControlPausableFacet.sol/contract.AccessControlPausableFacet.md#rolepaused) event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to pause."
      }
  ]}
/>

### unpauseRole

Re-enables a role that was previously paused. Only the admin of the role can unpause it. Emits a [RoleUnpaused](/src/access/AccessControlPausable/AccessControlPausableFacet.sol/contract.AccessControlPausableFacet.md#roleunpaused) event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="function unpauseRole(bytes32 _role) external;"
  description="Re-enables a role that was previously paused. Only the admin of the role can unpause it. Emits a [RoleUnpaused](/src/access/AccessControlPausable/AccessControlPausableFacet.sol/contract.AccessControlPausableFacet.md#roleunpaused) event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to unpause."
      }
  ]}
/>

### requireRoleNotPaused

Checks if an account has a role and if the role is not paused. - error: AccessControlUnauthorizedAccount If the account does not have the role. - error: AccessControlRolePaused If the role is paused.

<APIReference
  method="POST"
  endpoint="function requireRoleNotPaused(bytes32 _role, address _account) external view;"
  description="Checks if an account has a role and if the role is not paused. - error: AccessControlUnauthorizedAccount If the account does not have the role. - error: AccessControlRolePaused If the role is paused."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to check."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to check the role for."
      }
  ]}
/>

## Events

### RolePaused

Event emitted when a role is paused.

```solidity
event RolePaused(bytes32 indexed _role, address indexed _account);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_role` | `bytes32` | The role that was paused. |
| `_account` | `address` | The account that paused the role. |

### RoleUnpaused

Event emitted when a role is unpaused.

```solidity
event RoleUnpaused(bytes32 indexed _role, address indexed _account);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_role` | `bytes32` | The role that was unpaused. |
| `_account` | `address` | The account that unpaused the role. |

## Errors

### AccessControlUnauthorizedAccount

Thrown when the account does not have a specific role.

```solidity
error AccessControlUnauthorizedAccount(address _account, bytes32 _role);
```

### AccessControlRolePaused

Thrown when a role is paused and an operation requiring that role is attempted.

```solidity
error AccessControlRolePaused(bytes32 _role);
```

## Structs

### AccessControlStorage

```solidity
struct AccessControlStorage {
    mapping(address account => mapping(bytes32 role => bool hasRole)) hasRole;
    mapping(bytes32 role => bytes32 adminRole) adminRole;
}
```

### AccessControlPausableStorage

```solidity
struct AccessControlPausableStorage {
    mapping(bytes32 role => bool paused) pausedRoles;
}
```

