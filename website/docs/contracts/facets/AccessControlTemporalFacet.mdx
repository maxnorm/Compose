---
sidebar_position: 99
title: "AccessControlTemporalFacet"
description: "API documentation for AccessControlTemporalFacet"
---

import DocHero from '@site/src/components/docs/DocHero';
import Callout from '@site/src/components/ui/Callout';
import APIReference from '@site/src/components/api/APIReference';

<DocHero
  title="AccessControlTemporalFacet"
  subtitle="API documentation for AccessControlTemporalFacet"
  variant="gradient"
  height="small"
/>

## Overview

Documentation for AccessControlTemporalFacet.

[View Source](https://github.com/maxnorm/Compose/blob/ff0903436c4e0119526128efec4dad880d20e793/src/access/AccessControlTemporal/AccessControlTemporalFacet.sol)

## Functions

### getAccessControlStorage

Returns the storage for AccessControl.

<APIReference
  method="GET"
  endpoint="function getAccessControlStorage() internal pure returns (AccessControlStorage storage s);"
  description="Returns the storage for AccessControl."
  parameters={[]}
  response={{
      "s": "The AccessControl storage struct."
  }}
/>

### getStorage

Returns the storage for AccessControlTemporal.

<APIReference
  method="GET"
  endpoint="function getStorage() internal pure returns (AccessControlTemporalStorage storage s);"
  description="Returns the storage for AccessControlTemporal."
  parameters={[]}
  response={{
      "s": "The AccessControlTemporal storage struct."
  }}
/>

### getRoleExpiry

Returns the expiry timestamp for a role assignment.

<APIReference
  method="GET"
  endpoint="function getRoleExpiry(bytes32 _role, address _account) external view returns (uint256);"
  description="Returns the expiry timestamp for a role assignment."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to check."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to check."
      }
  ]}
  response={{
      "result": "The expiry timestamp, or 0 if no expiry is set."
  }}
/>

### isRoleExpired

Checks if a role assignment has expired.

<APIReference
  method="GET"
  endpoint="function isRoleExpired(bytes32 _role, address _account) external view returns (bool);"
  description="Checks if a role assignment has expired."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to check."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to check."
      }
  ]}
  response={{
      "result": "True if the role has expired or doesn't exist, false if still valid."
  }}
/>

### grantRoleWithExpiry

Grants a role to an account with an expiry timestamp. Only the admin of the role can grant it with expiry. Emits a RoleGrantedWithExpiry event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="function grantRoleWithExpiry(bytes32 _role, address _account, uint256 _expiresAt) external;"
  description="Grants a role to an account with an expiry timestamp. Only the admin of the role can grant it with expiry. Emits a RoleGrantedWithExpiry event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to grant."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to grant the role to."
      },
      {
          "name": "_expiresAt",
          "type": "uint256",
          "required": true,
          "description": "The timestamp when the role should expire (must be in the future)."
      }
  ]}
/>

### revokeTemporalRole

Revokes a temporal role from an account. Only the admin of the role can revoke it. Emits a TemporalRoleRevoked event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="function revokeTemporalRole(bytes32 _role, address _account) external;"
  description="Revokes a temporal role from an account. Only the admin of the role can revoke it. Emits a TemporalRoleRevoked event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to revoke."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to revoke the role from."
      }
  ]}
/>

### requireValidRole

Checks if an account has a valid (non-expired) role. - error: AccessControlUnauthorizedAccount If the account does not have the role. - error: AccessControlRoleExpired If the role has expired.

<APIReference
  method="POST"
  endpoint="function requireValidRole(bytes32 _role, address _account) external view;"
  description="Checks if an account has a valid (non-expired) role. - error: AccessControlUnauthorizedAccount If the account does not have the role. - error: AccessControlRoleExpired If the role has expired."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to check."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to check the role for."
      }
  ]}
/>

## Events

### RoleGrantedWithExpiry

Event emitted when a role is granted with an expiry timestamp.

```solidity
event RoleGrantedWithExpiry(
    bytes32 indexed _role, address indexed _account, uint256 _expiresAt, address indexed _sender
);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_role` | `bytes32` | The role that was granted. |
| `_account` | `address` | The account that was granted the role. |
| `_expiresAt` | `uint256` | The timestamp when the role expires. |
| `_sender` | `address` | The account that granted the role. |

### TemporalRoleRevoked

Event emitted when a temporal role is revoked.

```solidity
event TemporalRoleRevoked(bytes32 indexed _role, address indexed _account, address indexed _sender);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_role` | `bytes32` | The role that was revoked. |
| `_account` | `address` | The account from which the role was revoked. |
| `_sender` | `address` | The account that revoked the role. |

## Errors

### AccessControlUnauthorizedAccount

Thrown when the account does not have a specific role.

```solidity
error AccessControlUnauthorizedAccount(address _account, bytes32 _role);
```

### AccessControlRoleExpired

Thrown when a role has expired.

```solidity
error AccessControlRoleExpired(bytes32 _role, address _account);
```

## Structs

### AccessControlStorage

```solidity
struct AccessControlStorage {
    mapping(address account => mapping(bytes32 role => bool hasRole)) hasRole;
    mapping(bytes32 role => bytes32 adminRole) adminRole;
}
```

### AccessControlTemporalStorage

```solidity
struct AccessControlTemporalStorage {
    mapping(address account => mapping(bytes32 role => uint256 expiryTimestamp)) roleExpiry;
}
```

