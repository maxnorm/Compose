---
sidebar_position: 99
title: "AccessControlFacet"
description: "API documentation for AccessControlFacet"
---

import DocHero from '@site/src/components/docs/DocHero';
import Callout from '@site/src/components/ui/Callout';
import APIReference from '@site/src/components/api/APIReference';

<DocHero
  title="AccessControlFacet"
  subtitle="API documentation for AccessControlFacet"
  variant="gradient"
  height="small"
/>

## Overview

Documentation for AccessControlFacet.

[View Source](https://github.com/maxnorm/Compose/blob/c5fa0f9e7d2d9c835414fc63aa46e9da0ac324f9/src/access/AccessControl/AccessControlFacet.sol)

## Functions

### getStorage

```solidity
function getStorage() internal pure returns (AccessControlStorage storage s);
```

Returns the storage for the AccessControl.

<APIReference
  method="GET"
  endpoint="getStorage"
  description="Returns the storage for the AccessControl."
  parameters={[]}
  response={{
      "s": "The storage for the AccessControl."
  }}
/>

### hasRole

```solidity
function hasRole(bytes32 _role, address _account) external view returns (bool);
```

Returns if an account has a role.

<APIReference
  method="GET"
  endpoint="hasRole"
  description="Returns if an account has a role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to check."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to check the role for."
      }
  ]}
  response={{
      "result": "True if the account has the role, false otherwise."
  }}
/>

### requireRole

```solidity
function requireRole(bytes32 _role, address _account) external view;
```

Checks if an account has a required role. error: AccessControlUnauthorizedAccount If the account does not have the role.

<APIReference
  method="POST"
  endpoint="requireRole"
  description="Checks if an account has a required role. error: AccessControlUnauthorizedAccount If the account does not have the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to check."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to check the role for."
      }
  ]}
/>

### getRoleAdmin

```solidity
function getRoleAdmin(bytes32 _role) external view returns (bytes32);
```

Returns the admin role for a role.

<APIReference
  method="GET"
  endpoint="getRoleAdmin"
  description="Returns the admin role for a role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to get the admin for."
      }
  ]}
  response={{
      "result": "The admin role for the role."
  }}
/>

### setRoleAdmin

```solidity
function setRoleAdmin(bytes32 _role, bytes32 _adminRole) external;
```

Sets the admin role for a role. Emits a RoleAdminChanged event. error: AccessControlUnauthorizedAccount If the caller is not the current admin of the role.

<APIReference
  method="POST"
  endpoint="setRoleAdmin"
  description="Sets the admin role for a role. Emits a RoleAdminChanged event. error: AccessControlUnauthorizedAccount If the caller is not the current admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to set the admin for."
      },
      {
          "name": "_adminRole",
          "type": "bytes32",
          "required": true,
          "description": "The new admin role to set."
      }
  ]}
/>

### grantRole

```solidity
function grantRole(bytes32 _role, address _account) external;
```

Grants a role to an account. Emits a RoleGranted event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="grantRole"
  description="Grants a role to an account. Emits a RoleGranted event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to grant."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to grant the role to."
      }
  ]}
/>

### revokeRole

```solidity
function revokeRole(bytes32 _role, address _account) external;
```

Revokes a role from an account. Emits a RoleRevoked event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="revokeRole"
  description="Revokes a role from an account. Emits a RoleRevoked event. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to revoke."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to revoke the role from."
      }
  ]}
/>

### grantRoleBatch

```solidity
function grantRoleBatch(bytes32 _role, address[] calldata _accounts) external;
```

Grants a role to multiple accounts in a single transaction. Emits a RoleGranted event for each newly granted account. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="grantRoleBatch"
  description="Grants a role to multiple accounts in a single transaction. Emits a RoleGranted event for each newly granted account. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to grant."
      },
      {
          "name": "_accounts",
          "type": "address[]",
          "required": true,
          "description": "The accounts to grant the role to."
      }
  ]}
/>

### revokeRoleBatch

```solidity
function revokeRoleBatch(bytes32 _role, address[] calldata _accounts) external;
```

Revokes a role from multiple accounts in a single transaction. Emits a RoleRevoked event for each account the role is revoked from. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role.

<APIReference
  method="POST"
  endpoint="revokeRoleBatch"
  description="Revokes a role from multiple accounts in a single transaction. Emits a RoleRevoked event for each account the role is revoked from. error: AccessControlUnauthorizedAccount If the caller is not the admin of the role."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to revoke."
      },
      {
          "name": "_accounts",
          "type": "address[]",
          "required": true,
          "description": "The accounts to revoke the role from."
      }
  ]}
/>

### renounceRole

```solidity
function renounceRole(bytes32 _role, address _account) external;
```

Renounces a role from the caller. Emits a RoleRevoked event. error: AccessControlUnauthorizedSender If the caller is not the account to renounce the role from.

<APIReference
  method="POST"
  endpoint="renounceRole"
  description="Renounces a role from the caller. Emits a RoleRevoked event. error: AccessControlUnauthorizedSender If the caller is not the account to renounce the role from."
  parameters={[
      {
          "name": "_role",
          "type": "bytes32",
          "required": true,
          "description": "The role to renounce."
      },
      {
          "name": "_account",
          "type": "address",
          "required": true,
          "description": "The account to renounce the role from."
      }
  ]}
/>

## Events

### RoleAdminChanged

Emitted when the admin role for a role is changed.

```solidity
event RoleAdminChanged(bytes32 indexed _role, bytes32 indexed _previousAdminRole, bytes32 indexed _newAdminRole);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_role` | `bytes32` | The role that was changed. |
| `_previousAdminRole` | `bytes32` | The previous admin role. |
| `_newAdminRole` | `bytes32` | The new admin role. |

### RoleGranted

Emitted when a role is granted to an account.

```solidity
event RoleGranted(bytes32 indexed _role, address indexed _account, address indexed _sender);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_role` | `bytes32` | The role that was granted. |
| `_account` | `address` | The account that was granted the role. |
| `_sender` | `address` | The sender that granted the role. |

### RoleRevoked

Emitted when a role is revoked from an account.

```solidity
event RoleRevoked(bytes32 indexed _role, address indexed _account, address indexed _sender);
```

| Parameter | Type | Description |
|-----------|------|-------------|
| `_role` | `bytes32` | The role that was revoked. |
| `_account` | `address` | The account from which the role was revoked. |
| `_sender` | `address` | The account that revoked the role. |

## Errors

### AccessControlUnauthorizedAccount

Thrown when the account does not have a specific role.

```solidity
error AccessControlUnauthorizedAccount(address _account, bytes32 _role);
```

### AccessControlUnauthorizedSender

Thrown when the sender is not the account to renounce the role from.

```solidity
error AccessControlUnauthorizedSender(address _sender, address _account);
```

## Structs

### AccessControlStorage

```solidity
struct AccessControlStorage {
    mapping(address account => mapping(bytes32 role => bool hasRole)) hasRole;
    mapping(bytes32 role => bytes32 adminRole) adminRole;
}
```

