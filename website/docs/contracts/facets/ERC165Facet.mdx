---
sidebar_position: 99
title: "ERC165Facet"
description: "**Title:**"
---

import DocHero from '@site/src/components/docs/DocHero';
import Callout from '@site/src/components/ui/Callout';

<DocHero
  title="ERC165Facet"
  subtitle="**Title:**"
  variant="gradient"
  height="small"
/>

## Overview

**Title:**

ERC165Facet — ERC-165 Standard Interface Detection Facet


[View Source](https://github.com/maxnorm/Compose/blob/21dc7260ede2a70f647074cc724c78ca08dfafb9/src/interfaceDetection/ERC165/ERC165Facet.sol)




## Functions


### getStorage


Returns a pointer to the ERC-165 storage struct Uses inline assembly to bind the storage struct to the fixed storage position



```solidity
function getStorage() internal pure returns (ERC165Storage storage s);
```



**Parameters**

| Name | Type | Description |
|------|------|-------------|

| `getStorage` | `` | Returns a pointer to the ERC-165 storage struct Uses inline assembly to bind the storage struct to the fixed storage position |

### supportsInterface


Query if a contract implements an interface This function checks if the diamond supports the given interface ID



```solidity
function supportsInterface(bytes4 _interfaceId) external view returns (bool);
```



**Parameters**

| Name | Type | Description |
|------|------|-------------|

| `supportsInterface` | `` | Query if a contract implements an interface This function checks if the diamond supports the given interface ID |











## Structs


### ERC165Storage



```solidity
struct ERC165Storage {
    /// @notice Mapping of interface IDs to whether they are supported
    mapping(bytes4 =&gt; bool) supportedInterfaces;
}
```







